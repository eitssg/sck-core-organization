- version: v1
  metadata:
     name: core-organization-teardown
  description: Teardown the core organization Lambda function
  kind: delete-stack
  scope: portfolio
  params:
    stack_name: core-organization-lambda
    account: "{{ AutomationAccount }}"
    region: "{{ AutomationRegion }}"
  depends_on: []
